import React from 'react';

const AddressForm = ({ addressData, setAddressData }) => {
    return (
        <div className={'bg-white p-4 rounded-md grid grid-cols-2 gap-4'}>
            AddressForm
        </div>
    );
};

export default AddressForm;
